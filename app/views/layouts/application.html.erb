<!DOCTYPE html>
<html>
<head>
  <title>TaskProject</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  
  <div id="login">  
    <% if current_user %>
      <%= current_user.email %> (<%= link_to "Log out!", logout_path %>)
    <% else %>
      <%= link_to("Log In", login_path) %> | <%=link_to("Signup for an account", signup_path) %>
    <% end %>
  </div>
  
  <header>
  </header>

  <div id="main">
    
    <%= yield %>            
  </div>
  
  <footer>
  </footer>

</body>
</html>
